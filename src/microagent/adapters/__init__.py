"""Optional adapters that plug real model backends in as a Policy.

Adapters are kept OUT of the core so the framework itself has zero
dependencies. Import them only when you want a real LLM brain.
"""
